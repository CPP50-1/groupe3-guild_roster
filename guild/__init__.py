"""Fantasy Guild Roster - skeleton.

Fill in the TODOs. Each one is tagged with the day and (where relevant)
the developer it belongs to, matching the program:

    fields.py      -> Day 4 (descriptors, properties)
    exceptions.py  -> given, working (see its module docstring)
    items.py       -> Day 1 (data model: comparisons, equality, repr, hashing)
    models.py      -> Day 1 (dunders), Day 4 (MRO/mixins), Day 5 (metaclass registry)
    roster.py      -> Day 2 (container protocol, iterator protocol, callables)
    combat.py      -> Day 3 (generator send/throw/close)
    quests.py      -> Day 3 (itertools pipeline)
    dungeon.py     -> Day 3 (yield from delegation, lazy sequences, context manager)

Run `python -m pytest -v` at any point, tests for TODOs you haven't
reached yet are *expected* to fail. See README.md for which tests should
go green on which day, and for an important note about dependency order
between Day 1/2/3 and Day 4/5.
"""
